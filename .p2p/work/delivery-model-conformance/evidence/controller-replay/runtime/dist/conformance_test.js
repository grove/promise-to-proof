"use strict";
// Code generated by fizzbee-mbt generator; DO NOT EDIT.
Object.defineProperty(exports, "__esModule", { value: true });
const mbt_1 = require("@fizzbee/mbt");
const conformance_adapters_js_1 = require("./conformance_adapters.js");
// Registry keyed by role type ("" means top-level model actions)
const actionRegistry = new Map([
    ['', new Map([])],
    ['Controller', new Map([
            ['Start', async (inst, args) => {
                    return inst.actionStart(args);
                }],
            ['Review', async (inst, args) => {
                    return inst.actionReview(args);
                }],
            ['Proof', async (inst, args) => {
                    return inst.actionProof(args);
                }],
            ['Corrupt', async (inst, args) => {
                    return inst.actionCorrupt(args);
                }],
            ['Restart', async (inst, args) => {
                    return inst.actionRestart(args);
                }],
            ['Inspect', async (inst, args) => {
                    return inst.actionInspect(args);
                }],
            ['Resume', async (inst, args) => {
                    return inst.actionResume(args);
                }],
            ['Overlap', async (inst, args) => {
                    return inst.actionOverlap(args);
                }],
        ])],
]);
async function testConformance() {
    const model = (0, conformance_adapters_js_1.newConformanceModel)();
    const options = (0, conformance_adapters_js_1.getTestOptions)();
    try {
        await (0, mbt_1.runTests)(model, actionRegistry, options);
        model.cleanupAll();
    }
    catch (error) {
        console.error('Test failed:', error);
        process.exit(1);
    }
}
// Run tests
testConformance();
