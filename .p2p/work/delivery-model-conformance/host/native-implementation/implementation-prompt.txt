Invoke /Users/grove/.agents/skills/implement-contract/SKILL.md for the WHOLE canonical work/delivery-model-conformance.md. Read skill and protocol, applicable AGENTS and repository instructions. Do not delegate; do not invoke downstream review/proof. Your writable working root is the isolated implementation-source checkout shown by cwd. The source checkout /Users/grove/projects/promise-to-proof and controller records outside your cwd are read-only. No commits, pushes, tracker writes, external publication, destructive cleanup, or changes to canonical contract, binding specification, protocol, or repository Git metadata.

User requested $deliver-issue #30 and approved fresh sandboxed Codex CLI stages and gh network reads. Local implementation and safe temporary test repositories are authorized. Implement all R1-R8 completely, minimum complete solution, no unrelated phase or host support. Base a38045968c8d4d551a9e3a1f8bf14fed8bd4b481. .git points to a separate copied Git metadata directory outside your write scope. You may create fixture Git repos/commits within scratch for existing tests, but never commit product changes.

Before edits, read full contract, existing checks/delivery-model/{README.md,delivery.fizz,check.py}, actual controller skills/productivity/deliver-issue/scripts/p2p_delivery.py with callers, checks/test_p2p_delivery.py and check_p2p_delivery_host.py. Reuse controller and fixtures; FakeTransport may substitute agent replies only and must remain explicitly labeled. Actual controller processes, guards, stored state and CLI outputs must be exercised. Model restarts must create fresh controller processes. Cover all named contract cases, real overlapping CLI admission/resume, repair bound and fresh full verifier reports across restart, replayable failures, real controller guard mutations including stale-report rejection. An expected blocker string alone is weak: assert result AND saved state/effects. Never compute expected observations by copying production logic.

R1 compatibility evidence is retained in /Users/grove/projects/promise-to-proof/.p2p/work/delivery-model-conformance/evidence/compatibility; read its report first. That is the language-choice gate: select language only from successful real tool driver callback plus detected mismatch evidence. Scratch experiment at /Users/grove/projects/promise-to-proof/.p2p/tmp/issue-30/compatibility may supply working minimal configuration; copy only useful test code, no caches or binary archives into product. FizzBee v0.5.3 at /private/tmp/p2p-fizzbee-24/fizzbee-v0.5.3-macos_arm. Official MBT runtime extracted by the experiment has archive sha256 ae9296700d3b22aa67510cb5d3fe9e4f2b2cc2ecaa688cf14168cb5721888102; exact paths/pins in experiment report. Node/npm installed, Go absent. Use pinned generated adapter runtime dependencies and lockfile only if needed for real FizzBee integration; no general framework. Support configurable tool paths. Stage allows networking for localhost MBT and dependency reads, use a workspace-local npm cache, not home writes. Keep generated artifacts/caches out of product with targeted ignore entries.

Existing Phase1 model has finer Launch/Return/Save/ReadBack transitions than serial production run(). Explain the refinement from its invariants to observable controller operations; extend a bounded FizzBee model for missing admission/receipt/concurrency cases. Do not present a separate handwritten model or a list of tests disconnected from FizzBee-generated actions as conformance. Do not redesign production API just to fit model granularity; use existing actual public CLI and real boundary instrumentation in the test transport/subprocess where required. Preserve original baseline suite and document limits. MBT state/actions must cause actual operations, and get observed data from real persisted output. A targeted finite model of contract cases with preserved Phase1 rules is appropriate if its mapping and limitations are explicit.

Meaningful development checks: original controller tests, original 43-check model suite if relevant, new actual FizzBee MBT conformance, replay, mutant failures against unchanged independent oracles. Enclosing workflow can run live-host controller checks outside your stage sandbox (nested sandbox CLI may fail); if needed supply one exact command and material evidence gap instead of faking receipts. No worker-supplied independent=true may establish live provenance. Real host evidence from candidate controller will be retained by parent and independently inspected. If any implementation or check is incomplete, return PARTIAL honestly with concrete next action, preserve work, do not fabricate success.

Save useful evidence and report under your local .p2p/work/delivery-model-conformance/ (excluded from product). Use installed helper with --repo your isolated cwd for capture, explicit --base above. Retain exact final candidate snapshot/contract hash and full implementation report. Parent will capture/reconstruct and independently review/prove. Your final response is the full implementation report including all requirement coverage, actual test commands/results, exact changed paths and any limitations. Do not stop at a plan or ask permission for authorized local work. Continue until full implementation and available checks complete.
