# NOT PROVEN: Test the delivery controller against the FizzBee model

Requirements: 7/8  
Counterexamples tested: 15 distinct fault and mutation cases, plus one dedicated stale-report replay  
Contract: `work/delivery-model-conformance.md` v1  
Contract snapshot: SHA-256 `0751249534a5cf5f21d8f97250e95e4c91a3c5b27ec578f45fc5c21624167237`  
Binding source: `specs/delivery-model-conformance-source.md`, SHA-256 `7162a965fa53322a8805582756ea6137a847d72798e057dd751cc2e46c98f65a`  
Binding protocol: `docs/acceptance-contract-protocol.md`, SHA-256 `bec3503abc6ebc40356e662ca0b279be73a4da482eb1616a5c5b435509184351`  
Parent context: None; Phase 1 and Phase 2 are named prerequisites, not inherited contracts.  
Candidate: `snapshot:sha256:7a82e6bfc71d1e7fcf2318f9aa18473ed9a92e06c8f0d318b40b9be44dfcf912`  
Comparison base: `833a33647f545afb9028d03bf82d03613415ddf9`  
Candidate stability: unchanged; the saved key recomputed and all 126 manifest entries, including file modes and symlink targets, matched the fixed checkout before and after checks.  
Contract stability: unchanged; its exact SHA-256 matched before and after checks.  
Verification context: macOS arm64; `/opt/homebrew/bin/python3` 3.14.7; Node 26.9.0; npm 11.19.1; FizzBee 0.5.3; FizzBee MBT 0.2.0; TypeScript adapter with `@fizzbee/mbt` 0.1.2. All generated files, npm cache, temporary directories, and test Git metadata stayed under `/private/tmp/p2p-proof30`.

## Outcome

The agreement reconciles with the pinned Phase 3 handoff and the retained issue #30 body. R1–R8 cover the compatibility checkpoint, actual controller operations and persisted state, the Section 21.2 cases, real-guard mutations, overlap, replay, host-evidence distinctions, and documented bounds. I found no omitted or conflicting source promise and made no contract or plan-state changes.

The pinned conformance runner passed all 17 normal cases and caught all five deliberate production-guard mutations. The unchanged model checker passed its 43 finite checks, and the existing controller suite passed 19 tests. R1–R6 and R8 have candidate-bound evidence.

R7 remains **not proven**. The live-host records declare the exact candidate key and contain genuine host events, completion receipts, public controller JSON, saved state, review/proof reports, and denied protected-write observations. However, comparing the recorded `product_entries_sha256` against the saved candidate manifest found mismatches for **all 93 of 93 regular files**. For example, `p2p_delivery.py` is `cc7aa58ef786ac0bb6925fcefd19b181f21d48452a1c2df2906ca2953367e4af` in the candidate manifest and `ba0261a75a1c85cc20ce63b6659c9a2930142022876bca652917ac74a68de210` in the live-host receipt. The receipt's `code_unchanged: true` and `source_preserved: true` only describe that run; they do not resolve this mismatch. I cannot tie those live-host outcomes to this fixed candidate.

## Requirement verdicts

| ID | Observation and independent oracle | Evidence reference | Verdict |
|---|---|---|---|
| R1 | Before the TypeScript adapter choice, the pinned FizzBee driver invoked the real public `status` operation and accepted its `BLOCKED` result. Changing only the adapter observation to `STATUS_WRONG` failed at action 0 (`expected BLOCKED`, `actual STATUS_WRONG`); seed 42 reproduced it. Oracle: the driver's modeled `Status` return and the controller's public JSON. This is historical compatibility evidence, not evidence that the later full suite passed. | `/Users/grove/projects/promise-to-proof/.p2p/work/delivery-model-conformance/evidence/compatibility/README.md`, `commands.json`, `controller-good-verified.log`, `controller-bad-verified.log`, and `SHA256SUMS.json`. Fizz binary SHA-256 `8e8f905864b1781a3960f44fb654fc4455ef633e45556adf3fae586b652480a`; MBT server/runner SHA-256 values are recorded in the README and were rechecked by the final runner. | proven |
| R2 | FizzBee actions drove fresh Python controller processes through success, failure, restart, and repeated resume. `known-result-restart`, `uncertain-launch-restart`, `blocked-failure`, `repeated-resume`, and `repair-restart-exhaustion` all reached their required `Start`/`Restart`/`Resume`/review/proof action counts and compared returned JSON with saved delivery records. The exhausted-repair case retained `repair_used` after restart and dispatched no third repair. Oracle: the independently declared model transitions/invariants plus actual CLI return JSON and persisted state. | `/private/tmp/p2p-proof30/conformance-final/summary.json`; named case directories and `fixture/actions.jsonl`; assertions in the fixed `checks/delivery-model/conformance.py`. | proven |
| R3 | All ten required boundaries ran: `unauthorized-before-dispatch`, `stale-identity`, `mistyped-identity`, `candidate-mutated-during-verification`, `incomplete-archive`, `interrupted-report-storage`, `late-stage-result`, `duplicate-report-write`, `repeated-resume`, and `contested-dirty-file`. Each normal trace reached the required real CLI actions, ended in a checked `Resume`, and retained before/after state. Candidate mutation blocked reuse; `successful-repair` separately recorded new full review and proof reports whose exact candidate identity matched the repaired candidate. Oracle: contract identity/authorization rules and the independent FizzBee transition model compared with actual CLI JSON and saved records. | `/private/tmp/p2p-proof30/conformance-final/summary.json` and each corresponding case's `observation.json`, `fixture/actions.jsonl`, controller records, and MBT log. | proven |
| R4 | All five changes targeted real controller guards in disposable controller copies; the FizzBee model and adapter stayed unchanged. The stale-report, duplicate-report, late-receipt, repair-bound, and local-authority mutants each reached the real controller and produced the expected MBT return mismatch. A separate stale-report replay observed actual `1|BLOCKED|5|5|implementation,proof,review|0|1|1|-1|1` versus model-expected `1|BLOCKED|5|4|implementation,review|0|1|1|0|1`. The saved proof report's input identity had key `snapshot:sha256:7dd2708206e38a9f908f1bd53f3a6ff26a7245763bd43bbfa3666f73abde91d3`, while the current fixture candidate key was `snapshot:sha256:3dff31888a7276306b993cec029cc58ccf54be43e22a6c842f3f708c756a18f3`. Oracle: unchanged model and normalized state projection, including the persisted report bytes. | `/private/tmp/p2p-proof30/conformance-final/controller-*` mutation copies and case directories; dedicated replay `/private/tmp/p2p-proof30/stale-report-replay/stale-identity-42-stale-report-guard-removed/` (`mbt.log`, `fixture/actions.jsonl`, and saved records). | proven |
| R5 | `concurrent-resume` overlapped two fresh public controller processes. The losing process returned `BLOCKED` with `another controller holds the work-item lock` and left saved state unchanged; the winner advanced one review. The case used substitute worker replies, as declared. The retained live-host run launched five stages serially; it does not establish overlapping Codex stage execution. Oracle: both process results, lock admission, and one persisted state. | `/private/tmp/p2p-proof30/conformance-final/concurrent-resume-42/fixture/actions.jsonl` and `observation.json`; host limitation recorded in `/private/tmp/p2p-proof30/live-inspection.json`. | proven |
| R6 | The 17 case traces retain ordered actions, exact controller commands, returned JSON, before/after state, fixture inputs, model/MBT logs, and saved records. The stale-report failure was independently replayed with the same case, seed, and mutation and again failed on the same normalized mismatch. | `/private/tmp/p2p-proof30/conformance-final/` and `/private/tmp/p2p-proof30/stale-report-replay/`; exact replay command and observation are in `proof-observations.json`. | proven |
| R7 | Evidence classes are separately labeled. The live-host folder contains five unique `thread.started` IDs, each matched to the stage's controller session ID, launch record, zero-exit completion receipt, and (for implementation/review/proof) matching report hash. The actual public result and `delivery.json` both say `REVIEWED_AND_PROVEN`; raw probes recorded 28 denied protected-path writes each. But its declared product key is not supported by its file inventory: all 93 regular-file hashes differ from the saved manifest, including controller code and the conformance runner. Oracle: exact candidate manifest bytes/modes/symlinks and per-file hashes compared with `invocation.json` and `summary.json`, plus raw host events and receipts. | `/Users/grove/projects/promise-to-proof/.p2p/work/delivery-model-conformance/evidence/live-host/{invocation.json,summary.json,stdout.json}` and `tiny-source/.p2p/work/tiny/attempts/*/{events.jsonl,launch.json,exit.json,report.json}`; comparison in `/private/tmp/p2p-proof30/proof-observations.json`. | **not proven** |
| R8 | The candidate README documents action-to-public-operation mapping, oracle projections, all cases, tool pins/configurable paths, replay command, substitute/live distinctions, and finite exploration assumptions and limits. The unchanged model checker independently completed 43 checks: 11 finite baselines, 18 reachable witnesses, and 14 expected mutation counterexamples. This finite evidence does not establish all controller executions or host behavior. Oracle: R8's required documentation fields and the checker's asserted completion/output. | `/private/tmp/p2p-proof30/candidate/checks/delivery-model/README.md`; `/private/tmp/p2p-proof30/model-checks.log` and `model-checks/summary.json`. | proven |

## Verification commands and observations

- Full conformance: with `TMPDIR`, `TMP`, `TEMP`, `npm_config_cache`, and `PYTHONPYCACHEPREFIX` all set under `/private/tmp/p2p-proof30`, `PATH` prefixed with `/private/tmp/p2p-proof30/bin`, and `PYTHON=/opt/homebrew/bin/python3`, ran `/opt/homebrew/bin/python3 /private/tmp/p2p-proof30/candidate/checks/delivery-model/conformance.py --fizz /private/tmp/p2p-fizzbee-24/fizzbee-v0.5.3-macos_arm/fizz --mbt /Users/grove/projects/promise-to-proof/.p2p/tmp/issue-30/compatibility/fizzbee-mbt-0.2.0-macos_arm --output-dir /private/tmp/p2p-proof30/conformance-final --all-mutations`. It returned zero and `PASS: 22 traces`; summary lists 17 normal cases and five caught mutations with action counts. The runner used fresh `npm ci --ignore-scripts` from candidate `package-lock.json` SHA-256 `636d9e4cd80297565ee99102309e1970a45ca4bbd5747383940ffce903d412dd`, checked pinned FizzBee/MBT hashes, rejected execution/link errors, asserted each required enabled action count, and required a final `Resume`. Python 3.14.7 was passed to controller subprocesses. Logs and generated dependency files stayed in scratch.
- Dedicated stale guard replay: same environment and pins, with `--case stale-identity --seed 42 --mutation stale-report-guard-removed --output-dir /private/tmp/p2p-proof30/stale-report-replay`. It returned zero only because the controller defect produced the expected MBT mismatch; the exact expected/actual normalized observations and persisted stale report are captured above and in `proof-observations.json`.
- Original model checker: `/opt/homebrew/bin/python3 /private/tmp/p2p-proof30/candidate/checks/delivery-model/check.py --fizz /private/tmp/p2p-fizzbee-24/fizzbee-v0.5.3-macos_arm/fizz --output-dir /private/tmp/p2p-proof30/model-checks`; output: `PASS: 43 checks`. This is finite model evidence only.
- Existing controller suite: from the reconstructed scratch candidate, `/opt/homebrew/bin/python3 -m unittest checks.test_p2p_delivery`; output: `Ran 19 tests in 64.923s — OK`. These fixture tests supplement, and do not replace, model-based controller conformance.
- Final identity reread: `/opt/homebrew/bin/python3 /private/tmp/p2p-proof30/verify_final_inputs.py`; it recomputed the full snapshot key, matched all 126 fixed-checkout entries against the manifest, rechecked both binding inputs and agreement digest, and confirmed the base checkout `HEAD` equals the comparison base. The saved candidate record hash remained `7a3e5d09d6bc28131c7731d35121222216c2802aa0fa9100c693d34c13fd0d6a`.
- Live receipts: `/opt/homebrew/bin/python3 /private/tmp/p2p-proof30/inspect_live.py` independently matched each actual `thread.started` ID to its stage session ID and completion, matched report hashes, checked the saved public status, and counted protected-path denials. The product-entry mismatch audit is saved in `/private/tmp/p2p-proof30/proof-observations.json`.

The reconstructed candidate and all generated Git metadata were confined to scratch. The first all-mutations attempt stopped before mutation checks because that reconstructed copy lacked `.git`; after adding only scratch Git metadata, the fresh full run above passed. The pinned FizzBee launcher also requires a `mktemp` call; a scratch-only `PATH` shim directed that call into the approved temp directory. No product, contract, base, tracker, publication, or delivery Git metadata was written.

## Unresolved gaps

- R7: the retained live-host summary's exact product key matches, but its file-level source identity does not. Obtain a new actual-host invocation for the fixed snapshot and retain matching candidate-entry hashes, source/code stability, host-issued events, launch/completion receipts, public controller JSON, saved state, independent review/proof observations, and protected-write denials. The current live-host records are not evidence for this candidate until reconciled.
- Bounds: model-based runs use seed 42 and finite operation sequences. The documented finite checker and MBT cases are not proofs over all executions. Fixture worker replies and fixture session IDs do not establish live-host provenance. The live-host identity issue also prevents a candidate-specific isolation claim.

## Repairs needed

- R7 evidence only: refresh and bind live-host evidence to the exact candidate manifest; no product, contract, or base repair is established or made here.

Report storage: pending. This stage was restricted to `/private/tmp/p2p-proof30`; the enclosing workflow must copy the runnable observations and evidence directories into `.p2p/work/delivery-model-conformance/evidence/proof/`, then save and reread these exact report bytes. No report was saved outside scratch by this stage.

Fresh `/prove` is required after any repair or evidence refresh. Refresh `/review-implementation` only if the candidate changes.

Next steps:

1. After this exact report is saved and reread, use `/repair-gaps <saved proof>` to close the R7 evidence gap; this stage did not invoke repair.
2. Rerun `/prove` against candidate `snapshot:sha256:7a82e6bfc71d1e7fcf2318f9aa18473ed9a92e06c8f0d318b40b9be44dfcf912` once the actual-host receipt's per-file hashes match the complete saved manifest and its fresh receipts satisfy the observations above.